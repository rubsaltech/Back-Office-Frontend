# Netlify configuration for the RUBSAL Back-Office (Vite + React SPA)

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — let React Router handle all client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
