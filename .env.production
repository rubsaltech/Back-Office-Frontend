# Production build (npm run build), used by Render static site.
# Point this at your deployed Render backend. NO trailing slash. Must end with /api/v1.
# NOTE: Vite VITE_* vars are embedded in the built JS (public) — never put secrets here.
VITE_API_BASE_URL=https://rubsal-pos-api.onrender.com/api/v1
